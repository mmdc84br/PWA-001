// O Service Worker pode ser deixado vazio para testes básicos de instalação,
// ou você pode adicionar cache para funcionar offline depois.
self.addEventListener('fetch', (event) => {
    // Aqui poderíamos interceptar as requisições
});